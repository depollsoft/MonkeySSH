package main

import "time"

// replacementPaneGroup identifies a verified descendant of the outgoing server
// that led its own process group at capture. Keep wrappers before foreground jobs.
type replacementPaneGroup struct {
	pid     int
	started time.Time
}
